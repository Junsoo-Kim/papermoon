# 연극단 웹사이트 개발 가이드 (개발자 참고용)

> andamiroart.com은 디자인/구성 레퍼런스일 뿐, 실제로는 다른 연극단 사이트를 만든다는 전제로 정리.
> 스택: **Next.js (App Router) + Sanity + Vercel**

---

## 1. 만들어야 할 것 목록

- [ ] Next.js 프로젝트 (App Router, TypeScript)
- [ ] Sanity 프로젝트 + 스키마 설계 (공연 문서 타입)
- [ ] Sanity Studio를 `/studio` 라우트로 Next.js 앱에 내장
- [ ] 페이지: 홈 / 공연 목록 / 공연 상세 / 소개(ABOUT) / 단원(ARTIST) / 갤러리(MEMORY) / CONTACT
- [ ] 반응형 레이아웃 + 캐러셀/전환 애니메이션
- [ ] Vercel 배포 + 도메인 연결
- [ ] Sanity → Vercel 웹훅(자동 재빌드) 연결
- [ ] 환경변수 세팅

---

## 2. 프로젝트 초기화

```bash
npx create-next-app@latest theater-site --typescript --app --tailwind
cd theater-site
npm install sanity next-sanity @sanity/image-url @sanity/vision
```

- `sanity` : Studio 자체 (스키마, 관리 UI 엔진)
- `next-sanity` : Next.js와 Sanity 연결하는 공식 툴킷 (클라이언트, Studio 임베드 컴포넌트 포함)
- `@sanity/image-url` : 이미지 CDN URL 빌더
- `@sanity/vision` : Studio 안에서 GROQ 쿼리 테스트하는 플러그인 (개발 편의용, 없어도 됨)

---

## 3. 폴더 구조 (권장)

```
theater-site/
├── app/
│   ├── page.tsx                     # 홈
│   ├── performances/
│   │   ├── page.tsx                 # 공연 목록
│   │   └── [slug]/page.tsx          # 공연 상세
│   ├── about/page.tsx
│   ├── artists/page.tsx
│   ├── gallery/page.tsx             # MEMORY 역할
│   ├── contact/page.tsx
│   └── studio/[[...tool]]/page.tsx  # Sanity Studio 임베드 지점
├── sanity/
│   ├── schemaTypes/
│   │   ├── performance.ts
│   │   ├── artist.ts
│   │   └── index.ts
│   ├── env.ts
│   ├── client.ts
│   └── image.ts
├── sanity.config.ts
└── .env.local
```

---

## 4. Sanity 스키마 설계 (공연 문서 타입)

`sanity/schemaTypes/performance.ts`

```ts
import { defineField, defineType } from 'sanity'

export default defineType({
  name: 'performance',
  title: '공연',
  type: 'document',
  fields: [
    defineField({ name: 'title', title: '공연명', type: 'string', validation: r => r.required() }),
    defineField({ name: 'slug', title: 'URL 슬러그', type: 'slug', options: { source: 'title' }, validation: r => r.required() }),
    defineField({ name: 'date', title: '공연 일시', type: 'datetime' }),
    defineField({ name: 'venue', title: '장소', type: 'string' }),
    defineField({ name: 'summary', title: '한 줄 소개', type: 'text' }),
    defineField({ name: 'body', title: '상세 설명', type: 'array', of: [{ type: 'block' }] }),
    defineField({ name: 'mainImage', title: '대표 사진', type: 'image', options: { hotspot: true } }),
    defineField({
      name: 'gallery',
      title: '갤러리 사진',
      type: 'array',
      of: [{ type: 'image', options: { hotspot: true } }],
    }),
  ],
})
```

`sanity/schemaTypes/index.ts`

```ts
import performance from './performance'
import artist from './artist'

export const schemaTypes = [performance, artist]
```

`sanity/schemaTypes/artist.ts` — 단원은 여러 명이 추가/삭제되는 구조이므로 일반 문서 타입으로 만든다.

```ts
import { defineField, defineType } from 'sanity'

export default defineType({
  name: 'artist',
  title: '단원',
  type: 'document',
  fields: [
    defineField({ name: 'name', title: '이름', type: 'string', validation: r => r.required() }),
    defineField({ name: 'role', title: '직함/파트', type: 'string' }),
    defineField({ name: 'photo', title: '사진', type: 'image', options: { hotspot: true } }),
    defineField({ name: 'bio', title: '소개글', type: 'text' }),
  ],
})
```

`sanity/schemaTypes/about.ts` — 소개글은 페이지가 하나뿐이므로 "singleton(단일 문서)" 패턴으로 만든다. 문서를 여러 개 만들 수 없게 Studio 구조(desk structure)에서 고정해두면, 담당자는 "새로 만들기" 없이 항상 같은 문서 하나만 열어서 본문만 고치게 된다.

```ts
import { defineField, defineType } from 'sanity'

export default defineType({
  name: 'about',
  title: '단체 소개',
  type: 'document',
  fields: [
    defineField({ name: 'title', title: '제목', type: 'string' }),
    defineField({ name: 'body', title: '소개글', type: 'array', of: [{ type: 'block' }] }),
    defineField({ name: 'bodyEn', title: '소개글(영문)', type: 'array', of: [{ type: 'block' }] }),
  ],
})
```

`sanity.config.ts`의 `structureTool()` 설정에서 `about`을 singleton으로 고정하는 부분을 추가해야 실수로 여러 개 만들어지는 걸 막을 수 있다:

```ts
import { structureTool } from 'sanity/structure'

structureTool({
  structure: (S) =>
    S.list()
      .title('콘텐츠')
      .items([
        S.listItem().title('단체 소개').child(S.document().schemaType('about').documentId('about')),
        S.documentTypeListItem('performance').title('공연'),
        S.documentTypeListItem('artist').title('단원'),
      ]),
})
```

이렇게 해두면 담당자 입장에서는:
- **공연**: 새로 만들기 → 제목/소개/사진 채우고 저장
- **단원**: 새로 만들기(입단) 또는 삭제(탈퇴), 소개글 수정
- **단체 소개**: 딱 하나뿐인 문서 열어서 본문만 고치기

세 가지 화면만 오가면 되고, 페이지 구조나 코드는 건드릴 일이 없다.

---

## 5. Sanity Studio를 Next.js 앱에 내장

`sanity.config.ts` (프로젝트 루트)

```ts
import { defineConfig } from 'sanity'
import { structureTool } from 'sanity/structure'
import { visionTool } from '@sanity/vision'
import { schemaTypes } from './sanity/schemaTypes'

export default defineConfig({
  name: 'default',
  title: '연극단 관리자',
  projectId: process.env.NEXT_PUBLIC_SANITY_PROJECT_ID!,
  dataset: process.env.NEXT_PUBLIC_SANITY_DATASET!,
  basePath: '/studio',
  plugins: [structureTool(), visionTool()],
  schema: { types: schemaTypes },
})
```

`app/studio/[[...tool]]/page.tsx`

```tsx
'use client'
import { NextStudio } from 'next-sanity/studio'
import config from '../../../sanity.config'

export default function StudioPage() {
  return <NextStudio config={config} />
}
```

이렇게 하면 사이트 배포 시 `/studio` 경로로 관리자 화면이 같이 배포됨. 별도 배포 작업 불필요.

---

## 6. 프론트에서 데이터 가져오기

`sanity/client.ts`

```ts
import { createClient } from 'next-sanity'

export const client = createClient({
  projectId: process.env.NEXT_PUBLIC_SANITY_PROJECT_ID!,
  dataset: process.env.NEXT_PUBLIC_SANITY_DATASET!,
  apiVersion: '2024-01-01',
  useCdn: true, // 배포본은 CDN 캐시 사용 (속도↑)
})
```

`sanity/image.ts`

```ts
import imageUrlBuilder from '@sanity/image-url'
import { client } from './client'

const builder = imageUrlBuilder(client)
export const urlFor = (source: any) => builder.image(source)
```

공연 목록 페이지 예시 (`app/performances/page.tsx`):

```ts
import { client } from '@/sanity/client'

const query = `*[_type == "performance"] | order(date desc){
  title, slug, date, venue, summary, mainImage
}`

export default async function PerformancesPage() {
  const performances = await client.fetch(query)
  // performances.map(...) 으로 카드 그리드 렌더링
}
```

공연 상세 페이지는 `[slug]/page.tsx`에서 `*[_type == "performance" && slug.current == $slug][0]{...}` 형태로 단건 조회.

---

## 7. 반응형 · 사진 전환 연출

- 캐러셀/슬라이드: `embla-carousel-react` 또는 `swiper` — 스와이프 제스처, 페이드/슬라이드 전환 모두 기본 지원
- 등장 애니메이션·트랜지션: `framer-motion`
- 이미지: `next/image` + `urlFor(image).width(...).url()` 조합으로 반응형 사이즈 자동 서빙, `lazy` 로딩 기본 적용됨
- 라이트박스 확대보기: `yet-another-react-lightbox` 같은 가벼운 라이브러리 사용 가능

```bash
npm install embla-carousel-react framer-motion yet-another-react-lightbox
```

---

## 8. 환경변수 (.env.local)

```
NEXT_PUBLIC_SANITY_PROJECT_ID=xxxxxxx
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=xxxxxxx        # 필요 시(임시글 미리보기 등), 없어도 공개 데이터 읽기는 가능
```

`sanity/env.ts`에 모아서 export해두고 client.ts에서 import하는 식으로 관리하면 편함.

---

## 9. Vercel 배포

1. 로컬에서 `sanity init` 안 하고 위 스키마/설정 그대로 Git 저장소에 커밋 (Studio가 이미 Next.js 앱 안에 있으므로 별도 초기화 불필요, `sanity.config.ts`만 있으면 됨 — 단, 처음 한 번은 `npx sanity@latest login` 후 Sanity 프로젝트 자체는 `sanity.io/manage`에서 생성해서 projectId 발급받아야 함)
2. GitHub 저장소를 Vercel에 연결 (New Project → Import Git Repository)
3. Vercel 프로젝트 설정에서 위 환경변수 3개 등록 (Settings → Environment Variables)
4. Deploy 클릭 → `프로젝트명.vercel.app` 도메인으로 1차 배포 확인
5. Settings → Domains에서 실제 연극단 도메인 연결 (DNS에 CNAME/A 레코드 추가)

---

## 10. Sanity ↔ Vercel 웹훅 연결 (콘텐츠 등록 시 자동 재빌드)

1. Vercel 프로젝트 → Settings → Git → **Deploy Hooks** → 이름 입력 후 생성 → URL 복사
2. [sanity.io/manage](https://sanity.io/manage) → 해당 프로젝트 → API 탭 → **Create Webhook**
   - URL: 위에서 복사한 Vercel Deploy Hook URL 붙여넣기
   - Dataset: `production`
   - Trigger on: Create / Update / Delete 체크 (공연 문서 등록·수정·삭제 시 전부 반영되도록)
   - Filter(선택): `_type == "performance"` 로 좁히면 공연 문서 변경에만 반응하게 할 수 있음
3. Studio에서 공연 하나 등록 → Vercel Deployments 탭에서 새 빌드가 자동으로 뜨는지 확인

이 연결이 끝나면 이후로는 담당자가 Studio에서 등록/수정/삭제만 하면 사이트가 알아서 다시 빌드됨.

---

## 11. Studio 로그인 방식 (아이디/비번이 아님)

Sanity Studio는 일반적인 아이디/비밀번호 로그인이 아니라 **이메일 기반 로그인**이다.

- `sanity.io/manage` → 프로젝트 → Members에서 연극단 담당자 이메일을 멤버로 초대
- 담당자는 로그인할 때마다 해당 이메일로 오는 매직 링크를 클릭하거나, 구글 계정으로 로그인
- 비밀번호 자체가 없는 구조라 "비번 분실"이라는 상황이 발생하지 않는다

→ 인수인계 안내 문구는 "아이디/비번은 이거예요"가 아니라 **"이 이메일로 로그인하시면 되고, 로그인할 때마다 그 이메일로 링크가 와요. 비번은 따로 없어요"**로 전달해야 정확하다.

---

## 12. 완전히 손 떼기 위한 계정/소유권 체크리스트

"운영용 사이트 아이디/비번 드릴게요, 이후로는 연락 안 해도 됨" 시나리오가 성립하려면, 아래를 미리 정리해둬야 한다.

- [ ] **Sanity 프로젝트**: 연극단 담당자 이메일을 최소 멤버(가능하면 Administrator)로 추가. 본인 개인 계정에만 프로젝트가 묶여 있으면, 나중에 연락이 끊겼을 때 그쪽은 아무것도 못 건드림
- [ ] **Vercel 프로젝트**: 마찬가지로 연극단 쪽 계정/팀으로 소유권을 옮기거나 최소 협업자로 추가
- [ ] **도메인 갱신 책임자 지정**: 도메인은 누구 명의/카드로 등록했고, 갱신 시기가 언제인지 문서로 남겨서 전달. 이건 코드 유지보수는 아니지만 방치되면 사이트 자체가 내려감
- [ ] Vercel Hobby / Sanity Free 플랜 둘 다 신용카드 등록 없이 운영 가능 — 결제 문제로 서비스가 막힐 일은 없음

이 네 가지만 넘겨두면 "제목·내용·사진만 넣으면 올라가요, 이 이메일로 로그인하시면 되고, 궁금한 거 있으면 연락 주세요" 한 마디로 인수인계를 끝낼 수 있다.

---

## 13. 배포 전 체크리스트

- [ ] 모바일/태블릿/데스크톱 반응형 확인
- [ ] 공연 등록 → 실제 사이트 반영까지 전체 흐름 테스트
- [ ] 이미지 업로드 후 로딩 속도 확인 (Sanity CDN 통해 최적화되는지)
- [ ] 국문/영문 콘텐츠(있다면) 전환 확인
- [ ] `/studio` 접근 시 Sanity 로그인 요구되는지 (관리자만 접근 가능해야 함 — 기본적으로 Sanity 프로젝트 멤버만 로그인 가능)
- [ ] 도메인 SSL 정상 적용 확인 (Vercel이 자동 처리)

---

## 14. 인수인계 후 실제로 남는 유지보수 범위

위 1~13을 다 반영하면, 아래 세 가지는 **개발자 개입 없이 연극단 쪽에서 전담** 가능:

- 공연 추가/수정/삭제 (제목, 소개, 사진)
- 단원 추가/삭제, 소개글 수정
- 단체 소개글(국문/영문) 수정

개발자가 다시 필요한 경우는 사실상 아래뿐이고, 발생 안 해도 당장 문제 없는 것들이라 **"필요할 때만 연락받는" 수준**으로 봐도 된다:

- 공연/단원 항목 구조 자체를 바꾸고 싶을 때 (예: 티켓 링크 필드 추가)
- Next.js / Sanity 패키지 보안 업데이트가 필요할 때 (1~2년에 한 번 정도, 안 해도 즉시 사이트가 멈추진 않음)
- 페이지 디자인/레이아웃 자체를 새로 바꾸고 싶을 때

→ 인수인계 멘트로 정리하면: **"운영용 사이트예요. 여기다가 추가하고 싶은 연극 제목·내용·사진 넣으시면 자동으로 올라가요. 단원 정보나 소개글도 여기서 직접 고치시면 돼요. 로그인은 이 이메일로 하시면 되고 매번 이메일로 링크 와요. 궁금한 거 있으면 연락 주세요."**